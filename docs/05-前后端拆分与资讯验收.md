# 前后端拆分与资讯模块验收

2026-09-17，本机 Node.js 24.19.0 / Chrome。

## 已交付

- React + Vite 客户端渲染，移除 Next.js 依赖与服务端页面代码。根路由继续是完整平台首页。
- frontend/backend 两个独立 npm workspace；Node.js API 4000，静态前端本地预览 3000；生产分别构建为 Nginx / Node 容器。
- 既有知识、模型、工具、场景、教程、计算器、素材预算、比较、收藏、进度、搜索和备案信息保留。
- `/news` 与首页实时资讯：真实 RSS、原文链接、自动分类、搜索、来源筛选、分页、刷新、采集状态、首次收录和原始发布时间。
- 启动采集及默认 15 分钟定时检查，页面可见时每分钟获取列表。原子文件持久化、URL 去重、条件请求、超时及体积限制；不填充 mock 内容。

## 实际来源结果

首次采集 58 条真实记录：量子位 10、Google AI 20、NVIDIA 18、Microsoft Research 10。Hugging Face 订阅源已配置；当前机器 Node 网络连接超时，已在 UI 显示错误，没有将其标为采集成功。该数字为验收时快照，运行中会变化。来源连通性仍需 ECS 实机验证。

## 验证结果

- TypeScript、ESLint、313 条目录/42 厂商及词表 SHA-256 校验通过。
- 51 项单元测试通过（原计价/编码/收藏/内容测试 + 5 项资讯链接安全/去重/日期/分类测试）。
- 16 项桌面/移动端浏览器回归通过；确认 HTML 仅含 SPA 外壳、深层路由可打开、无 JS 提示可见，计算器正文不上传，收藏与进度恢复正常。
- 21 条路由 × 1440/375 两种宽度，共 42 页面检查，没有横向溢出和浏览器脚本异常。记录见 `platform-verification.json`；截图在 `screenshots/`。
- 资讯测试使用正在运行的真实 API；不拦截或注入模拟文章。

## 未进行与限制

ECS 未连接；本机无 Docker，未进行镜像运行验证。域名 HTTPS、云端境外源可达性、持久卷备份恢复仍待部署验收。JSON 存储仅支持单后端写入实例；水平扩容前迁移数据库。SPA 需要 JavaScript，正文不会出现在初始 HTML 中。CMS、账户、多端同步和全厂商资料审核不属于本轮完成项。
