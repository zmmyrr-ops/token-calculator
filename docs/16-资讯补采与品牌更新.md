# 资讯补采、百度文件验证与品牌更新

2026-09-17。

- ✅ 删除资讯页刷新按钮整行，保留请求失败时的重试操作及页面每分钟自动更新。
- ✅ 生产环境 NEWS_POLL_MINUTES=15，服务启动抓取一次，随后每 15 分钟自动抓取；不是每篇发布后立即抓取。
- ✅ 实测量子位官网在 17:07 发布的新文章尚未出现在当时 RSS 中；上一轮抓取约 17:00。因此既存在正常轮询窗口，也存在订阅源延迟。
- ✅ 增加量子位官网 h4 最新列表补采：仅允许固定 HTTPS 官网文章路径，最多检查 20 个列表链接、每轮补 10 篇缺失文章，并发 2。正文仅用于核对标题及北京时间发布时间，不转载全文。来源失败下轮重试。
- ✅ 量子位 RSS 每轮读取完整内容，不依赖其条件缓存；其他源继续保留 ETag / Last-Modified 优化。两种来源按原文 URL 去重。
- ✅ 原样部署用户提供的 baidu_verify_codeva-dNjacCJLDs.html，放在 frontend/public 中随 CI/CD 发布。
- ✅ Nginx 为 www 域名的精确验证路径保留 200，其他 www 页面继续跳转主域。
- ✅ 新 SVG 图标：深蓝底、青绿至淡紫的门框与开启的门，体现“AI 门道”。替换导航图标与浏览器 favicon。
- ✅ 新 slogan：“看懂 AI，用出门道。”用于首页主标题、页脚、HTML 描述及首页 SEO 描述。
- ✅ 78 项单元测试、类型、lint、模型目录检查通过，桌面/手机检查品牌与资讯页无溢出。

## 百度操作边界

用户已提供本地验证文件，因此直接按字节复制，不再重复下载。文件成功访问不等于百度已验证；需要在百度搜索资源平台登录账号后点击“完成验证”。本次浏览器控制服务两次返回 Sky Computer Use service startup request failed，无法操作用户登录会话。已请求在应用右侧打开百度资源平台，工具返回 queued；不能声称页面已实际展示或已完成百度验证。
