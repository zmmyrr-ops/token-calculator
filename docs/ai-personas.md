# AI 人格合集：功能、维护与发布

前台入口 `/personas`，首页和主导航均有入口。管理入口 `/admin/personas`，使用既有管理员账号及会话。

## 已实现

- 当前 V2：5 个性格鲜明的人格、二次元头像、四场景示例切换、本机收藏。旧 12 个官方人格归档下架。
- 统一问题的人工编写示例，明确不是真实模型生成。
- 三档强度；本次对话、Codex Skill、项目默认、全局默认四种导出。
- 完整提示词预览、剪切板复制、Markdown 下载、可复现配置分享链接、SVG 分享卡片。
- 页内安装、验证、卸载、恢复教程；`frontend/public/guides/ai-personas.md` 为下载版。
- 后台编辑名称、图标、简介、表达规则、示例和发布状态；乐观锁避免覆盖他人修改。
- SEO 标题、简介、站点地图、静态可读简介；复制/下载埋点不包含聊天正文。

## 数据与接口

SQLite `personas`：`id` 主键、`data` JSON、`revision` 整数、`updated_at` 时间。

首次启动通过 `INSERT OR IGNORE` 写入官方人格；之后部署不会覆盖管理员编辑或重新发布已下架条目。整体 SQLite 备份包含此表。内容种子在 `backend/src/personas.ts`，校验与导出规则在 `shared/personas.ts`。

- GET `/api/v1/personas`：只返回已发布人格。
- GET `/api/admin/personas`：管理员读取全部内容。
- PUT `/api/admin/personas/:id`：提交 `{data, revision}`，需管理员登录并通过既有来源校验。ID 不可修改；版本冲突 409；修改事务同时写入 `history`。

没有接入生成模型或额外模型密钥，没有虚构用户数、热度或下载量。示例是编辑内容；V2 强度切换同时选择预先编写的对应示例和导出指令；不是实时模型生成。生成的人格文件需用户主动安装，网站不会直接改本地配置。

## 维护

后台点击“AI 人格管理”，选择条目后编辑，保存立即生效。下架从列表移除；旧分享链接显示不可用提示并提供其他已发布人格。收藏仅存浏览器，过期 ID 不展示。

编辑时注意：名称与简介要与规则一致；台词回答同一个示例问题；不得把示例描述成模型实测。默认配置只影响沟通风格，不应覆盖用户项目规范和权限。

## 验证与发布

`npm run check`：类型、lint、单元测试和既有小程序检查。
`npx playwright test tests/e2e/personas.spec.ts tests/e2e/admin.spec.ts`：桌面/移动浏览器的配置、下载、分享还原、收藏和后台编辑。

按现有 main → GitHub Actions → staging → production 同镜像提升流程发布。没有修改小程序功能入口；该模块是网站独立页面。

## 本次发布记录

2026-09-18：版本 `610233f473069a7843171190b0ee7f3882631dff` 已部署测试和生产环境。
CI：https://github.com/zmmyrr-ops/token-calculator/actions/runs/35302501271 。
96 项单元测试通过；桌面/移动端的人格与后台流程验证通过。发布前已自动备份数据库。

## 专属头像与详细人格设定（2026-09-18）

12 个人格使用独立绘制的成年虚构角色插画，源提示词见 `docs/persona-art-prompts.md`。网页头像保存在 `frontend/public/persona-avatars/`，使用版本化 WebP 文件名；列表、配置区、页头及管理后台共用头像，分享 SVG 内嵌图片，不依赖在线资源。

数据库 `personas.data.instructions` 保存可编辑的完整人格正文，涵盖角色定位、语言节奏、处理方法、禁忌及技术排错、产品取舍、情绪支持、停止人格四类示范。后台「完整人格设定与多场景示范」可编辑（100–12000 字符），所有复制及下载模式使用同一份正文。

启动时只为缺少 instructions 的旧记录补充设定，保留管理员修改和上下架状态；补充时增加 revision，避免旧页面覆盖新内容。已有详细设定不重复覆盖。三档强度附加具体风格密度要求，正式交付内容遵守用户要求。示范为人工编写，不能保证所有模型完全一致，建议在目标模型中试聊后微调。

发布验证：d44564f7d5c11da30ac02157e4583eeedb763eb1 已部署测试与生产。CI 35304756853 全量检查通过；98 项单元测试、桌面及手机端后台/人格用例通过。正式 API 返回 12 套完整设定，正式浏览器确认 12 张头像加载成功且无运行错误。头像 WebP 合计约 412 KiB。


## V2 数据与维护（2026-09-21）

新种子：`backend/src/personas-v2.ts`。`voice` 包含风格标签、默认称呼、三档名称与规则、四场景分档示例、三组三轮对话。后台可编辑这些字段，复制/下载使用数据库当前内容。网页预览与导出共用 `personaText`、`personaExample` 和 `personaArtifact`。

迁移 `personas-v2-20260921`：同一事务内为旧 12 个官方 ID 设 published=false、增加 revision、写 history；备份原行至 `persona_content_migrations.snapshot`，插入 5 个新种子。不修改自定义条目、不覆盖后续编辑，不更改 AI 眼里的你。发布时另有整个数据库快照。

需要恢复旧内容时：先备份当前库并停写，读取该迁移 snapshot，逐行恢复对应旧 ID 的 data（revision 应在当前值上递增，避免旧表单覆盖）；如果仍运行 V2 程序，将 V2 五个条目下架并保留迁移记录，防止下次启动再次归档旧条目。如果要回退到不认识 voice 字段的旧程序，须先将五个新条目的完整行另存快照，然后移除其 data.voice 并下架，才能通过旧程序的严格 JSON 校验；以后重新升级时再从该快照恢复新版字段和发布状态。仅回退镜像不足以恢复人格内容。不要恢复整库覆盖发布后产生的其他业务数据。

验收包含一次迁移、重复初始化、管理员自定义条目保留、恢复快照内容、revision 冲突、分强度示例、称呼替换、分享与导出、旧链接提示。目标模型实际表达随客户端而异，页面示例不代表四个平台均已实测。

V2 发布记录：2026-09-21，`43ac5149094081e9df8f1f5fe438006c8ef2b3ce` 经 CI 35581431368 验证并部署 staging，再使用同镜像提升 production。发布前数据库备份成功；前后端健康，API 返回五个新角色，头像校验、正式网页强度切换和旧链接提示验证通过。
